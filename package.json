{
  "name": "ckeditor",
  "version": "1.0.0",
  "description": "ckeditor",
  "license": "Apache-2.0",
  "devDependencies": {
    "bower": "^1.7.7",    
    "watch-http-server": "^0.7.6",
    "http-server": "^0.9.0"
  },
  "scripts": {
    "postinstall": "bower install",
    "prestart": "npm install",
    "start": "watch-http-server -a localhost -p 8000 -c-1 ./app"
  }
}
